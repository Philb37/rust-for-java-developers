info:
  name: Status
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseurl}}/tickets/:id/status"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "status": "IN_PROGRESS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
