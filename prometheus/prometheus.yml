global:
  scrape_interval: 5s
  evaluation_interval: 5s

# Scrape configurations
scrape_configs:
  - job_name: 'telegraf'
    static_configs:
      - targets: ['telegraf:9273']  

storage:
  tsdb:
    out_of_order_time_window: 5m